type Query {

    authors: [Author]
    author(id: ID) : Author
}
type Book {
    id: ID
    name: String
    pageCount: Int
}

type Author{
    id: ID
    firstName: String
    lastName: String
    book: Book
}